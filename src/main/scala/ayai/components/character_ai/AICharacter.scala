package ayai.components

import com.artemis.Component
import com.artemis.Entity

class AICharacter(characterId : String, x : Int, y : Int, range : Int, ai_system : AISystem) extends Character(characterId, x, y) {
	
}