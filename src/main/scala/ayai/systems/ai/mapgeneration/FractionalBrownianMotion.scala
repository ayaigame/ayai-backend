package ayai.systems.mapgenerator

import java.util.Random

// class BadConfigException(msg: String) extends RuntimeException(msg)

object FractionalBrownianMotion {
  // def getFractalNoise(noiseType: String, width: Int, height: Int, octaves: Int): Array[Array[Double]] = {
    
  // }
}